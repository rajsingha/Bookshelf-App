<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Inactive state -->
    <item android:color="@color/off_blue" android:state_checked="false" />

    <!-- Active state -->
    <item android:color="@color/blue" android:state_checked="true" />
</selector>
